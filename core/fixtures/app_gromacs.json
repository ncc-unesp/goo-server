[
    {  
        "fields": {
            "name": "gromacs-4.5.4-sp-serial.zip",
            "url": "gsiftp://se.grid.unesp.br/store/gridunesp/goo/apps/gromacs-4.5.4-sp-serial.zip",
            "object_type": "A",
            "create_time": "2013-03-28T20:18:10.500Z",
            "user": 1,
            "public": true,
            "size": 4841685
        },
        "model": "storage.object",
        "pk": 10
    },
    {
        "fields": {
            "_app_obj": 10,
            "args": "--v -nt ${cores_per_host} -s ${name}.tpr -o ${name}.trr -e ${name}.edr -c after_${name}.gro -g ${name}.log",
            "checkpoints": "",
            "_constant_fields": "executable, hosts",
            "cores_per_host": 1,
            "diskspace": 2048,
            "executable": "",
            "hosts": 1,
            "inputs": "${name}.tpr",
            "maxtime": 43200,
            "memory": 2048,
            "_multi_hosts": false,
            "_multi_thread": true,
            "_name": "Gromacs",
            "_version": "4.5.4 Single Precision Serial",
            "outputs": "${name}.trr ${name}.edr after_${name}.gro ${name}.log",
            "_required_fields": "cores_per_host, maxtime",
            "_shared_fs": false,
            "_description": "GROMACS is a versatile package to perform molecular dynamics, i.e. simulate the Newtonian equations of motion for systems with hundreds to millions of particles. (http://www.gromacs.org)",
            "_usage": "The default values expect input file named ${name}.tpr"
        },
        "model": "core.application",
        "pk": 10
    }
]
